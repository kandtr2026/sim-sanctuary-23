# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.next/

# TypeScript incremental build cache, written by `tsc -b`.
*.tsbuildinfo

# Local environment. Kept out of git even though every VITE_ var is public once
# built — see .env.example for what to set and src/integrations/supabase/config.ts
# for the fallbacks that let a fresh clone run without it.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Ảnh/video thô do bộ gen media trả về. Chỉ bản đã nén trong public/blog mới
# được commit (xem scripts/blog-images/optimize.mjs).
generated-media/
